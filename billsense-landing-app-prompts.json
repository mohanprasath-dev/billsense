{
  "BillSense — Landing Page + App Rebuild (Apple Fluid Design, Light Theme)": [
    {
      "step": 1,
      "title": "Design System Setup",
      "prompt": "Reference apple-design.md (provided) as the design skill for this entire rebuild. Set up a light-theme-only design system in the project: CSS variables for a calm health-tech palette (soft white/off-white backgrounds, muted teal or blue as primary accent, reserve red only for the 'overpriced' price flag). Typography per apple-design.md section 15: system-ui font stack, size-specific tracking (negative letter-spacing on large display text, near-zero on body), leading that tightens on headings and loosens on body copy. Install Motion (Framer Motion) for spring-based animation — critically damped (damping 1.0, response 0.3-0.4) as the default for all UI transitions; reserve slight bounce (damping ~0.8) only for momentum-driven interactions like the camera capture button press or card reveals triggered by a swipe/drag. Respect prefers-reduced-motion (cross-fade fallback, no springs) per section 14.",
      "ui_details": "No visual output yet — tokens/config only. Confirm palette and type scale before moving to step 2."
    },
    {
      "step": 2,
      "title": "Landing Page — Hero + Nav",
      "prompt": "Build a new app/(marketing)/page.tsx as the public landing page (separate from the app tool itself, which moves to /app). Sticky top nav, translucent on scroll (apple-design.md section 12: backdrop-filter blur, bright top edge border) with: BillSense wordmark left, nav links (How it works, About, Team), a primary 'Try the Demo' button right that routes to /app. Hero section: large headline stating the core value prop in plain language (e.g. 'Understand your medical bill in seconds'), subheadline explaining the OCR → explain → price-check flow, primary CTA button 'Try the Demo' with pointer-down instant feedback (scale 0.97 on press, per section 1), and a small badge/label reading 'Live Demo — Full Product in Development' so expectations are set immediately, not buried.",
      "ui_details": "Respond on pointer-down not click release for all buttons. Nav must collapse to a simple mobile menu below 640px. Hero CTA should feel like the obvious single next action — one primary button, no competing CTAs."
    },
    {
      "step": 3,
      "title": "Landing Page — How It Works + Problem/Solution",
      "prompt": "Add a 'How it works' section below the hero: 3-4 step visual flow (Upload/Photo → AI reads it → Plain explanation → Price check), each step as a card with icon, short label, one-line description — apply section 16 'Simplicity': concise language, no jargon. Above or below it, add a short 'Why this exists' section stating the real problem in one or two sentences (patients don't understand hospital bills, price transparency is inconsistent) — keep factual, no invented statistics unless you give me real ones to use. Use scroll-triggered reveal animations for each card (fade + slight upward motion), respecting reduced-motion fallback (opacity-only cross-fade).",
      "ui_details": "Cards in a horizontal row on desktop, stacked vertical scroll on mobile. Reveal one card at a time as it enters viewport, not all at once — reinforces the step-by-step narrative."
    },
    {
      "step": 4,
      "title": "Landing Page — Team + Contact Section",
      "prompt": "Add a 'Team' section with two members: 'Mohan Prasath' (Founder & Developer) and 'Thejashree J P' (Team Member). Simple card layout per person: name, role, no bio paragraph needed unless one is provided later. Below or beside the team section, add a 'Contact' block showing only Mohan's contact details: Instagram @thetaskdrift, https://www.taskdrift.in/, info.taskdrift@gmail.com, mohanprasath.dev — do not list a contact method for Thejashree. Style this section understated, not a heavy 'About Us' wall of text — per section 16 Craft, every element should earn its place.",
      "ui_details": "Two-column or two-card layout for the team, side by side on desktop, stacked on mobile. Contact links as simple tappable rows/icons, each with pointer-down feedback."
    },
    {
      "step": 5,
      "title": "App Screen — Camera Capture (Best UX)",
      "prompt": "On the /app tool screen, replace the plain file-upload drop zone with a proper live camera capture flow, best-UX approach: use getUserMedia to open a live camera preview inline (not just a native file picker), rear camera by default (facingMode: 'environment') on mobile, with a capture button styled as a camera shutter. On capture, freeze the frame, show a review screen with 'Retake' and 'Use This Photo' actions before submitting for analysis. Fallback: if getUserMedia is unavailable or permission is denied, gracefully fall back to a standard file input with capture='environment' attribute so upload still works. Follow apple-design.md section 1 (respond on press, not release) for the shutter button, and section 2 (direct manipulation) — no lag between tap and visual capture feedback (flash/shutter animation on capture).",
      "ui_details": "Full-screen camera preview on mobile, contained card on desktop (desktop rarely has a rear camera, so also show a clear 'Upload instead' link). Shutter button large, centered, bottom of preview, thumb-reachable. Review screen shows the captured photo large with Retake (secondary) and Use This Photo (primary) side by side."
    },
    {
      "step": 6,
      "title": "App Screen — Apply Design System to Results",
      "prompt": "Re-skin the existing results screen (test cards, price flags, share card, language toggle) using the light-theme design system and typography rules from step 1 — this is a pass on existing functionality, not new features. Ensure the price-flag colors (green/amber/red) meet contrast requirements (per section 14, don't rely on color alone — keep icon + text label). Apply critically-damped spring transitions when cards appear (staggered reveal, one after another, not all at once) instead of any instant/no-animation state.",
      "ui_details": "Cards stagger in with ~60-80ms delay between each, spring-based (damping 1.0), not CSS keyframes. Disclaimer bar and language toggle keep prior functionality, just re-themed to light palette."
    },
    {
      "step": 7,
      "title": "Global Polish + Reduced Motion Pass",
      "prompt": "Final pass across landing page and app: verify every interactive element (buttons, nav, camera shutter, share button) responds on pointer-down with instant visual feedback per section 1. Add prefers-reduced-motion media query overrides globally — springs and scroll-reveals become simple opacity cross-fades, camera shutter flash becomes a plain state change with no animation. Verify the 'Live Demo — Full Product in Development' badge/message from step 2 is visible on both landing and app screens so it's clear throughout that this is a hackathon demo, not the finished product.",
      "ui_details": "No new UI — QA pass. Test on mobile viewport primarily since camera capture and touch feedback matter most there."
    }
  ]
}
